// ignore_for_file: constant_identifier_names

enum ViewsType { main, favorite }

enum SnackBarType { normal, error }

enum Gender { FEMALE, MALE, UNKNOWN, GENDERLES }

enum Species { ALIEN, HUMAN }

enum Status { ALIVE, DEAD, UNKNOWN, ALL }
